// operação ternaria é como se fosse o iif do delphi

const valor = -1;

const negativoOuPositivo = valor > 0 ? 'Positivo' : 'Negativo'; // o que vem depois do ? é a condição True e depois de : a condição false
console.log(negativoOuPositivo);


