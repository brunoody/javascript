const { resolve } = require('path');
module.exports = {
  config: resolve(__dirname, 'src', 'config', 'database.js'),
  'models-path': resolve(__dirname, 'src', 'models'),
  'migrations-path': resolve(__dirname, 'src', 'database', 'migrations'), // alterações feitas na base de dados de forma "versionda', temporal..
  'seeders-path': resolve(__dirname, 'src', 'database', 'seeds'),// as pastas migrations e seeds serao criadas automaticamente

};

